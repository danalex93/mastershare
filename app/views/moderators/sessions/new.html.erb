<div class="ui middle aligned center aligned two column grid">
  <div class="column">
    <h2 class="ui green image header">
      <%= image_tag "logo.png", class: "image" %>
      <div class="content">
        Iniciar Sesión como Moderador
      </div>
    </h2>
    <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "ui large form" }) do |f| %> 
      <div class="ui stacked segment">
        <div class="field">
          <div class="ui left icon input">
            <i class="user icon"></i>
            <%= f.email_field :email, autofocus: true, placeholder: "Correo Electrónico", autocomplete: "email" %>
          </div>
        </div>
        <div class="field">
          <div class="ui left icon input">
            <i class="lock icon"></i>
            <%= f.password_field :password, placeholder: "Contraseña", autocomplete: "off" %>
          </div>
        </div>
        <%= f.submit "Iniciar Sesión", class: "ui fluid large submit green button" %>
      </div>

      <div class="ui error message"></div>

    <% end %>

    <div class="ui message">
      <%= render "moderators/shared/links" %>
    </div>
  </div>
</div>
